<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.rk_aiz.teamsurvey.infrastructure.mapper.mybatis.AnswerPatternItemMapper">

    <resultMap id="AnswerPatternItemMap" type="com.github.rk_aiz.teamsurvey.infrastructure.entity.AnswerPatternItemEntity">
        <id property="id" column="id" />
        <result property="answerPatternId" column="answer_pattern_id" />
        <result property="itemText" column="item_text" />
        <result property="itemOrder" column="item_order" />
    </resultMap>

    <select id="selectByPatternId" resultMap="AnswerPatternItemMap">
        SELECT id, answer_pattern_id, item_text, item_order
        FROM answer_pattern_items
        WHERE answer_pattern_id = #{patternId}
        ORDER BY item_order
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO answer_pattern_items (answer_pattern_id, item_text, item_order)
        VALUES (#{answerPatternId}, #{itemText}, #{itemOrder})
    </insert>

    <update id="update">
        UPDATE answer_pattern_items
        SET answer_pattern_id = #{answerPatternId},
            item_text = #{itemText},
            item_order = #{itemOrder}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM answer_pattern_items
        WHERE id = #{id}
    </delete>

    <delete id="deleteByPatternId">
        DELETE FROM answer_pattern_items
        WHERE answer_pattern_id = #{patternId}
    </delete>

</mapper>